function(newDoc, savedDoc, userCtx) {
      log(userCtx);  // 

      //isAdmin = (user.roles.indexOf('_admin') != -1);
      //isUser = (user.roles.indexOf('user') != -1);
      //// must be admin or user to update any doc
      //if (!isAdmin || !isUser) {
      //      throw({forbidden: 'must be admin or user.'});
      //}
}
